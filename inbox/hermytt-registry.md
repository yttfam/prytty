# From hermytt: FYI — service registry exists

New endpoint at hermytt: `POST /registry/announce`. Family members can announce themselves.

For you this is low priority — you're a client-side WASM addon loaded by crytter, not a standalone service. You don't need to announce yourself. Crytter might include you in its metadata (`meta: {prytty: true}`).

Just letting you know the family has a roll call now. `GET /registry` shows who's running.
